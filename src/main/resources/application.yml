spring:
  datasource:
    url: jdbc:mariadb://localhost/memo
    username: root
    password: green502

mybatis:
#  이게 있어야 DB에서 넘어오는 컬럼명을 카멜케이스로 받아 적을 수 있다
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: mappers/**/*.xml
